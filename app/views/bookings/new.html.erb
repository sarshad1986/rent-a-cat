<div class="container">
  <h1>Create your booking with the adorable <%= @cat.name %></h1>

<%= simple_form_for [@cat, @booking] do |f| %>
  <%= f.input :date_from, as: :date, html5: true%>
  <%= f.input :date_to, as: :date, html5: true %>
  <%= f.button :submit %>
<% end %>