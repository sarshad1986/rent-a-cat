<div class="container">
  <div class="index-banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= asset_path("pair-cats-cuddling.jpg") %>');">
    <h1 class="index-heading">Browse cats</h1>
  </div>
  <%# <% @cats.each do |cat| %>
    <%# <li><%= link_to cat.name, cat_path(cat) %>
  <%# <% end %>
  <%= render 'shared/cards_index' %>
</div>
